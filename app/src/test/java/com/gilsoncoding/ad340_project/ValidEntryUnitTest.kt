package com.gilsoncoding.ad340_project

import org.junit.Test

import org.junit.Assert.*

class ValidEntryUnitTest {

    @Test
    fun validateLogin() {
    // after some attempts at getting tests to work for validateLogin but not getting the code
    //   to recognize the method, I deleted the test file. But then I couldn't launch the app
    //   because it still wanted to find the test file, so I had to create this dummy file to
    //   get it to cooperate.
    }
}